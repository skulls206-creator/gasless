You are updating an ALREADY-BUILT Tron USDT “gasless” web wallet app.
Do NOT start a new project. Work only with the existing codebase.Current situation (assume this is already working):Frontend can create/import Tron wallets (address + private key or similar).Users can see their USDT (TRC‑20) balances.There is a backend that can build and broadcast normal USDT TRC‑20 transfers on Tron.I already have (or will have) a sponsor wallet on the backend that holds TRX / resources to pay network costs.Goal:
Add a fee system so that:Every time a user sends USDT from my web app,The app charges a fixed 1 USDT service fee,That 1 USDT goes to my sponsor/fee wallet (an address I control), andThe user STILL does NOT need any TRX in their own wallet (the sponsor wallet pays/backs the network fees like now).Very important:This is an incremental update. Reuse my existing send-USDT logic and APIs.Do NOT remove or break any of the current features.Keep the flow as close as possible to what I already have.Implementation details I want:Fee configIntroduce a clear config value for the fee, for example:FEE_IN_USDT = 1.0FEE_RECIPIENT_ADDRESS = "<MY_SPONSOR_WALLET_ADDRESS>"Put this in a central config file or environment variables so I can change it later without rewriting code.Backend send flow
Update the backend endpoint/function that sends USDT (for example /send-usdt or similar) to:Validate that the user has at least:amount_to_send + FEE_IN_USDT in their USDT balance.Build the transaction(s) so that:The intended recipient gets exactly amount_to_send USDT.The fee wallet (FEE_RECIPIENT_ADDRESS) receives FEE_IN_USDT USDT in the same flow.If you need to send two separate TRC‑20 transfers (one to the recipient, one to the fee wallet), that is acceptable, but:Make sure both sends use the same gasless/sponsor mechanism as the current implementation.Make sure the backend uses my sponsor wallet / delegated resources to cover the Tron gas, so the user still doesn’t need TRX.Safety checks and errorsIf the user does NOT have enough balance for amount_to_send + FEE_IN_USDT, return a clear error that the user does not have enough USDT to cover the fee.If any part of the fee transfer fails, the whole send should fail (don’t send to the recipient without collecting the fee).Frontend changesIn the send screen:Show a line like: “Service fee: 1.00 USDT (no TRX required)”.Show a “Total debited” value = amount_to_send + FEE_IN_USDT.Make sure the confirmation modal clearly shows:“You send: X USDT to [recipient]”“Fee: 1.00 USDT to service wallet”“Total deducted from your wallet: X + 1.00 USDT”Code clarityKeep function and variable names descriptive (e.g., feeAmount, feeRecipient, totalAmountDebited).Add minimal comments right where the fee is applied explaining what the fee does and why it exists.What I want you to outputShow the updated backend code for the send-USDT endpoint/function, with the fee logic fully integrated.Show any necessary config/env changes.Show the minimal frontend changes (components or hooks) needed to:Display the fee and total.Handle new error messages (like “not enough balance including fee”).